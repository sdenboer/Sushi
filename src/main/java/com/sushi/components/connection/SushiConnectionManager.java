package com.sushi.components.connection;

public class SushiConnectionManager {

//    private static final Logger logger = LogManager.getLogger(SushiConnectionManager.class);
//
//    public void send(SushiOrder sushiOrder) {
//
//        final FileReceiver fileReceiverAsync = new FileReceiver(sushiOrder.sushiHost().getPort(), 2, TARGET, fileWriter -> {
//            pass.set((fileWriter.done() && (LARGE_FILE_ONE.contains(fileWriter.getFileName()) || LARGE_FILE_TWO.contains(fileWriter.getFileName()))));
//            jobsLatch.countDown();
//        });
//
//    }
}
